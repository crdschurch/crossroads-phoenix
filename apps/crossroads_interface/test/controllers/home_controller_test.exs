defmodule CrossroadsInterface.HomeControllerTest do
  use CrossroadsInterface.ConnCase
  #TODO home controller is not in use at the moment
#  test "GET /", %{conn: conn} do
    #conn = get conn, "/"
    #assert html_response(conn, 200)
  #end
end
