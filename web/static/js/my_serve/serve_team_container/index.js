import CONSTANTS from 'crds-constants';

import serveTeamContainerComponent from './serveTeamContainer.component';

export default angular.
module(CONSTANTS.MODULES.MY_SERVE).
component('serveTeamContainer', serveTeamContainerComponent())
;
