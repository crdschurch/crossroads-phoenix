import CONSTANTS from 'crds-constants';

import closableTagComponent from './closableTag.component';
import html from './closableTag.html';

export default angular.
module(CONSTANTS.MODULES.MY_SERVE).
component('closableTag', closableTagComponent())
;
